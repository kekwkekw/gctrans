# Relay bootstrap

Temporary branch-only marker used solely to open the draft PR required by Relay Protocol v1.0. It must be deleted before pre-merge PASS. It is not archive data and must never reach main.
