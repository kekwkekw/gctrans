# R15 schema v1 문맥 확장
core schema/enums는 변경하지 않는다.
- 일반 main11 본문578+제목10을 등록했다. 검수 완료는 승인과 별개다.
- 신규 canonical entity/character profile은 0이다. 기존 인물 프로필의 상대·시점 관찰을 추가한다.
- 프라워맨 두 엔티티는 동일 문자열의 실제 화자(계승자)와 언급 대상(전임자)으로 구분한다.
- 동일 귀족 라벨의 여러 출현과 전각 시민Ａ/Ｂ, 반각 시민A/B를 로컬 문맥으로 구분한다. 문자 모양만이 아니라 시점·대상 근거가 우선이다.
- ???의 후향 식별은 테레사로 연결하되 7화25의 공개 근거 및27의 source 라벨 전 원문을 변경하지 않는다.
- 부친은 사망 뒤 CONTROLLED_CORPSE 발화 모드다. 부친의 생전 대사와 섞지 않는다.
- 고흐가 네이트의 조언을 전하는10화39는 실제 speaker 고흐, reported_speaker 네이트로 분리한다.
- 별도 battle raw에는 제목 행이 없고 battle_row_index가0부터다. 이번에는 원본 구조 snapshot만 보존하며 새 source entry/번역/승인/runtime 출력은 없다.
